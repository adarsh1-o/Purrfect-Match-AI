# Testing Strategy

- **Unit Tests**:
  - `test_matching.py`: Verifies weight variations for apartment constraints and work hours.
  - `test_ai.py`: Asserts movement tracker coordinates parsing.
- **Integration Tests**: Next.js typescript validation run during production builds (`npm run build`).
